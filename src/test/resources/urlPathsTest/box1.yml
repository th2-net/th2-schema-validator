apiVersion: th2.exactpro.com/v2
kind: Th2Box
metadata:
  name: box1
spec:
  extendedSettings:
    service:
      enabled: true
      clusterIP:
        - name: port1
          containerPort: 8080
          urlPath: URL1
        - name: port2
          containerPort: 8080
          urlPath: URL1
        - name: port3
          containerPort: 8080
          urlPath: URL2