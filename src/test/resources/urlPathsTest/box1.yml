apiVersion: th2.exactpro.com/v2
kind: Th2Box
metadata:
  name: box1
spec:
  extendedSettings:
    service:
      enabled: false
      ingress:
        urlPaths:
          - URL1
          - URL1
          - URL2